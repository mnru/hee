package com.github.kputnam.bee.terms

/**
 * Parent class common to literal values (terms which always yield
 * themselves when evaluated, thus don't need evaluation)
 */
abstract class LiteralTerm extends Term
