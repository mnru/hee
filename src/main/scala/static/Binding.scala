package com.github.kputnam.bee.static

import com.github.kputnam.bee.types.Type
import com.github.kputnam.bee.kinds.Kind

sealed abstract class Binding

/**
case class NameBinding extends Binding

case class TermVarBinding(τ: Type) extends Binding
case class TermAbstractionBinding(e: Term, τ: Type) extends Binding

case class TypeVarBinding(k: Kind) extends Binding
case class TypeAbstractionBinding(τ: Type, k: Kind) extends Binding
**/
