package com.github.kputnam.bee.nodes

/**
 * Parent class common to literal values (nodes which always yield
 * themselves when evaluated, thus don't need evaluation)
 */
abstract class LiteralNode extends AbstractNode
